{
  "name": "delle-model-lp",
  "version": "1.0.0",
  "description": "Delle Model landing page",
  "private": true,
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
